# Cập nhật tiến độ

## PROJECT_SUMMARY.md

Bổ sung:

```text
- Đã phát hiện Drizzle CLI chưa đọc `.env.local`.
- Đã sửa `drizzle.config.ts` để load `.env.local` trước `.env`.
- Sprint 0B.1 chưa PASS cho đến khi `db:push` và `db:seed:auth` chạy thành công.
```

## MASTER_CHECKLIST.md

Cập nhật:

```text
- [x] Sửa Drizzle CLI đọc `.env.local`.
- [-] Chạy `pnpm db:push`.
- [ ] Chạy `pnpm db:seed:auth`.
- [ ] Kiểm tra cột mới và dữ liệu seed.
```
